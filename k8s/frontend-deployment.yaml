apiVersion: apps/v1
kind: Deployment
metadata:
  name: resonant-frontend
  labels:
    app: resonant-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: resonant-frontend
  template:
    metadata:
      labels:
        app: resonant-frontend
    spec:
      containers:
      - name: frontend
        image: resonant-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
